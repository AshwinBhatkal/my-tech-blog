import colors from "./colors";

export const theme = {
    dark: {
        background: {
            body: colors.secondary,
            header: colors.black,
            footer: colors.black,
        },
        color: {
            primary: colors.primary,
            activeLink: colors.white,
            link: colors.primary,
        },
    },
};
